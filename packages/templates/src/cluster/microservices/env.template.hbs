# Environment configuration for {{project.name}} cluster
# Generated: {{metadata.timestamp}}
#
# Copy this file to .env and fill in your values:
#   cp .env.template .env

# === Authentication ===
GITHUB_TOKEN=
ANTHROPIC_API_KEY=

# === Repository ===
REPO_URL={{repos.primary}}
REPO_BRANCH={{defaults.baseBranch}}

# === Cluster ===
WORKER_COUNT=3
REDIS_URL=redis://redis:6379

# === Docker-in-Docker ===
ENABLE_DIND=true
