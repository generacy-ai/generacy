name: Publish Cluster Microservices Image

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch to build (develop or main)"
        type: choice
        options:
          - develop
          - main
        required: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          repository: generacy-ai/cluster-microservices
          ref: ${{ inputs.ref }}

      - name: Determine image tags
        id: tags
        run: |
          if [ "${{ inputs.ref }}" = "develop" ]; then
            echo "channel=preview" >> "$GITHUB_OUTPUT"
          else
            echo "channel=stable" >> "$GITHUB_OUTPUT"
          fi
          echo "sha=sha-$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/generacy-ai/cluster-microservices:${{ steps.tags.outputs.channel }}
            ghcr.io/generacy-ai/cluster-microservices:${{ steps.tags.outputs.sha }}
