# Stack notes: #1218 spec-stage agent-context guard

Generated by /plan — do not hand-edit.

- **Packages**: `@generacy-ai/orchestrator` (worker `pr-manager.ts`, guard logic),
  `@generacy-ai/workflow-engine` (`GitHubClient` interface + `GhCliGitHubClient`, new
  `revertPaths`; `managed-file-disjointness.test.ts` Layer-1 removal).
- **Language/runtime**: TypeScript ESM, Node >= 22; vitest for tests.
- **No new dependencies.** Revert uses git CLI via the existing `executeCommand` helper in
  `gh-cli.ts` plus `node:fs/promises` for untracked-file deletion.
- **Build order**: workflow-engine before orchestrator (cross-package imports resolve to
  `dist/`).
- **Release**: single changeset — workflow-engine `minor` (new public interface method),
  orchestrator `patch` (defect fix).
