Generated by /plan — do not hand-edit.

# Stack: 1204-context-integration-issue

- **Deliverable type**: Markdown validation artifacts (runbook, results report, criteria
  contract) under `specs/1204-context-integration-issue/`; no new runtime code.
- **Optional tooling**: small committed stanza-diff helper (POSIX shell) at
  `specs/1204-context-integration-issue/scripts/diff-gateway-stanza.sh`.
- **Conditional code surface**: `packages/generacy/src/cli/commands/cluster/scaffolder.ts`
  (TypeScript, ESM, Node >=22) — touched only if a stanza divergence is traced to the
  scaffolder; requires a `patch` changeset.
- **Validation runtime (operator-executed, not in-repo)**: Bifrost LLM gateway
  (`maximhq/bifrost:v2.0.0`, pinned), docker compose cluster templates, published
  `@generacy-ai/generacy` CLI + cluster-base image, generacy-cloud staging deploy.
- **Key integration points**: gateway route = model string containing `/` via second
  `CLAUDE_CONFIG_DIR`; subscription route unchanged; canon compose stanza at
  `tetrad-development/.devcontainer/generacy/docker-compose.yml:180-218`.
- **Blocking upstream contracts (all OPEN as of 2026-08-28)**: generacy#1202,
  cluster-base#90, generacy-cloud#919; cockpit route rule deferred to agency#510 (P4).
- **No dependency changes**: no new npm packages, no compose changes on this branch
  unless a scaffolder fix lands.
