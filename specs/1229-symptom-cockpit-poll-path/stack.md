Generated by /plan — do not hand-edit.

# Stack: 1229-symptom-cockpit-poll-path

- **Language/runtime**: TypeScript (ESM), Node >= 22.
- **Packages touched**:
  - `@generacy-ai/cockpit` (`packages/cockpit`) — new `GhWrapper.batchLookupIssuesOrPrs`
    interface method + `GhCliWrapper` implementation in `src/gh/wrapper.ts` (aliased
    GraphQL `issueOrPullRequest(number:)` batch, mirrors `buildTier1FollowupQuery` /
    `tier1FollowupOnce`).
  - `@generacy-ai/generacy` (`packages/generacy`) — `cli/commands/cockpit/watch/poll-loop.ts`
    (exact lookup + defensive post-filter, `PollDeps` slimmed), new
    `cli/commands/cockpit/shared/ref-set-filter.ts`, `cli/commands/cockpit/status.ts`
    (post-filter), caller/test updates.
- **Dependencies**: none added. `zod` (existing, `packages/cockpit`) validates the GraphQL
  response envelope. GitHub access stays via the `gh` CLI through the existing
  `CommandRunner` seam (`gh api graphql`).
- **Test framework**: vitest; fakes via `__tests__/helpers/fake-gh.ts` (gains the new
  method) and scripted `CommandRunner` in
  `cockpit-graphql-budget.integration.test.ts` (updated to intercept `api graphql`).
- **Build note**: cross-package imports resolve to built `dist/` — rebuild
  `@generacy-ai/cockpit` before typechecking/testing `packages/generacy`.
- **Notable decisions** (full rationale in `research.md`):
  - Exact-lookup GraphQL replaces free-text `gh search issues` on the poll path (clarify
    Q2=C); returns PRs, reviving the poll path's PR branch (Q1=A).
  - Defensive `repo#number` post-filter (lowercased repo, matching `snapshotKey` #1106
    normalization) applied on the poll path and to `status.ts` search results.
  - NOT_FOUND GraphQL errors tolerated (stale epic-body refs); all other errors throw.
  - Chunk ≤100 aliases per call; empty ref set ⇒ zero calls (sentinel query removed).
  - Budget: per-repo search/list calls decrease (1 GraphQL call replaces paginated REST
    search); gated per-PR calls accepted per clarify Q3=A, pinned by the existing budget
    integration test.
  - Changeset: `@generacy-ai/cockpit` minor (new public API), `@generacy-ai/generacy`
    patch (defect fix).
