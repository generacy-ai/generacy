Generated by /plan — do not hand-edit.

# Stack: #1228 doorbell answer replay scoping + consumed cursor

- **Language/runtime**: TypeScript (ESM), Node >= 22.
- **Package touched**: `packages/generacy` (`@generacy-ai/generacy`) only.
- **Dependencies**: no new packages. Reuses `@generacy-ai/cockpit` (`resolveEpic`,
  `GhWrapper`, `ResolvedEpic` — rebuild its `dist/` before typechecking dependents),
  `zod` (cursor-file schema), `node:fs/promises` via the existing injectable `FsFacade`.
- **New modules**: `doorbell/ref-set-holder.ts` (shared `EpicRefSetHolder`),
  `doorbell/answers-cursor-store.ts` (atomic tmp+rename cursor persistence).
- **Modified**: `doorbell/answers-file-source.ts` (ref-set scope test, cursor-driven
  replay/resume, recency window), `doorbell/smee-source.ts` (delegates ref-set
  resolve/store to the holder), `doorbell.ts` (wiring + `COCKPIT_ANSWERS_REPLAY_WINDOW_MS`
  env parse).
- **Config surface**: new env `COCKPIT_ANSWERS_REPLAY_WINDOW_MS` (default 24 h); cursor
  files at `<answersDir>/cursors/<owner>__<repo>__<n>.json` on the `generacy-workspace`
  volume, beside `COCKPIT_ANSWERS_FILE`.
- **Notable decisions**: advance-on-emit at-most-once cursor (no session ack channel
  exists); miss-triggered ref-set refresh throttled to 30 s; hermetic harness mode
  (`COCKPIT_DOORBELL_HARNESS=1`, no gh) keeps the legacy owner/repo compare; frozen
  down-path `GateAnswerLine` wire shape untouched.
- **Testing**: vitest, fake-`FsFacade` seams, suites under
  `packages/generacy/src/cli/commands/cockpit/doorbell/__tests__/`.
- **Release**: patch changeset for `@generacy-ai/generacy` (defect fix) — required by the
  changeset CI gate.
