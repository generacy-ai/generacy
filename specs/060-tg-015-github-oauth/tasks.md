# Tasks: GitHub OAuth Authentication

**Input**: Design documents from `/specs/060-tg-015-github-oauth/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, quickstart.md
**Status**: Complete

## Format: `[ID] [P?] [Story] Description`
- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to

## Phase 1: Data Model & Types

- [x] T001 Define `AuthTier` enum (`auth.ts:12-19`)
- [x] T002 [P] Define `AuthUser` interface (`auth.ts:24-41`)
- [x] T003 [P] Define `AuthToken` interface (`auth.ts:46-55`)
- [x] T004 [P] Define `AuthState` interface (`auth.ts:60-69`)
- [x] T005 [P] Define `AuthChangeEvent` interface (`auth.ts:74-81`)
- [x] T006 Define storage keys constant (`auth.ts:86-91`)

## Phase 2: Core AuthService

- [x] T010 Implement singleton pattern with `getInstance()` (`auth.ts:103-127`)
- [x] T011 Implement `initialize()` with URI handler registration (`auth.ts:132-161`)
- [x] T012 [P] Implement state accessors: `getState()`, `isAuthenticated()`, `getTier()`, `getUser()` (`auth.ts:166-199`)
- [x] T013 Implement `hasMinimumTier()` for tier comparison (`auth.ts:194-199`)
- [x] T014 Implement `dispose()` for cleanup (`auth.ts:319-326`)

## Phase 3: OAuth Flow

- [x] T020 Implement `login()` with browser redirect (`auth.ts:204-240`)
- [x] T021 Implement `handleOAuthCallback()` URI handler (`auth.ts:339-380`)
- [x] T022 Implement `exchangeCodeForTokens()` API call (`auth.ts:385-472`)
- [x] T023 Implement CSRF state generation (`auth.ts:695-699`)
- [x] T024 Implement callback promise handling (`auth.ts:715-745`)

## Phase 4: Token Management

- [x] T030 Implement `storeTokens()` to SecretStorage (`auth.ts:477-487`)
- [x] T031 [P] Implement `clearTokens()` cleanup (`auth.ts:492-498`)
- [x] T032 Implement `getAccessToken()` with expiry check (`auth.ts:279-304`)
- [x] T033 Implement `refreshToken()` API call (`auth.ts:503-565`)
- [x] T034 Implement `handleTokenExpired()` with user notification (`auth.ts:570-600`)
- [x] T035 Implement `scheduleTokenRefresh()` timer (`auth.ts:605-624`)
- [x] T036 [P] Implement `clearTokenRefresh()` timer cleanup (`auth.ts:629-634`)

## Phase 5: Session Restoration

- [x] T040 Implement `restoreSession()` from storage (`auth.ts:639-671`)
- [x] T041 Implement `updateContext()` for VS Code when clauses (`auth.ts:676-683`)

## Phase 6: Event System

- [x] T050 Implement `onDidChange()` listener registration (`auth.ts:309-314`)
- [x] T051 Implement `notifyListeners()` event dispatch (`auth.ts:704-712`)
- [x] T052 Implement `logout()` with event emission (`auth.ts:245-274`)

## Phase 7: Commands

- [x] T060 Register cloud commands in `registerCloudCommands()` (`cloud.ts:21-48`)
- [x] T061 Implement `handleLogin()` command (`cloud.ts:77-123`)
- [x] T062 [P] Implement `handleLogout()` command (`cloud.ts:128-151`)
- [x] T063 [P] Implement `handleShowAccount()` command (`cloud.ts:156-247`)
- [x] T064 Implement `initializeCloudServices()` (`cloud.ts:53-72`)
- [x] T065 Implement `handleAuthChange()` listener (`cloud.ts:252-271`)
- [x] T066 Implement `requiresTier()` guard function (`cloud.ts:306-340`)

## Phase 8: Tests

- [x] T070 Set up vscode mock for tests (`auth.test.ts:5-62`)
- [x] T071 Test singleton instance management (`auth.test.ts:121-135`)
- [x] T072 [P] Test initial anonymous state (`auth.test.ts:137-152`)
- [x] T073 [P] Test `initialize()` behavior (`auth.test.ts:154-174`)
- [x] T074 Test `hasMinimumTier()` logic (`auth.test.ts:176-188`)
- [x] T075 Test `logout()` state clearing (`auth.test.ts:190-218`)
- [x] T076 [P] Test `getAccessToken()` when not authenticated (`auth.test.ts:220-226`)
- [x] T077 Test `onDidChange()` listener management (`auth.test.ts:228-250`)
- [x] T078 Test login flow browser opening (`auth.test.ts:253-268`)

## Phase 9: Integration

- [x] T080 Export auth module from `api/index.ts`
- [x] T081 Add `CONTEXT_KEYS.isAuthenticated` to constants
- [x] T082 Wire cloud services in extension activation

## Dependencies & Execution Order

**Sequential dependencies**:
- Phase 1 → Phase 2 (types before service)
- Phase 2 → Phase 3, 4, 5, 6 (service before features)
- Phase 3 + 4 → Phase 5 (OAuth + tokens before session restore)
- Phase 2 → Phase 7 (service before commands)
- Phase 2-6 → Phase 8 (implementation before tests)

**Parallel opportunities**:
- T002-T005 can run in parallel (independent interfaces)
- T012 accessors can run in parallel with T013
- T031, T036 can run in parallel with T030, T035
- T062, T063 can run in parallel (independent commands)
- T072-T078 can run in parallel (independent test cases)

---

*Generated by speckit*
