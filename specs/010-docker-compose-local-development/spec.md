# Feature Specification: Docker Compose local development setup

**Branch**: `010-docker-compose-local-development` | **Date**: 2026-01-20 | **Status**: Draft

## Summary

## Summary

Create the docker-compose configuration for local development.

## Parent Epic

#7 - Generacy Services

## Dependencies

- #8 - Orchestrator service
- #9 - Worker service

## Requirements

### docker-compose.yml

```yaml
version: '3.8'

services:
  orchestrator:
    build:
      context: .
      dockerfile: services/orchestrator/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - REDIS_URL=redis://redis:6379
      - LOG_LEVEL=debug
    depends_on:
      - redis
    volumes:
      - ./services/orchestrator/src:/app/src:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  worker:
    build:
      context: .
      dockerfile: services/worker/Dockerfile
    environment:
      - NODE_ENV=development
      - REDIS_URL=redis://redis:6379
      - ORCHESTRATOR_URL=http://orchestrator:3000
      - LOG_LEVEL=debug
    depends_on:
      - redis
      - orchestrator
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./services/worker/src:/app/src:ro
    deploy:
      replicas: 2

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  redis-data:

networks:
  default:
    name: generacy-network
```

### Development Overrides

`docker-compose.override.yml`:
```yaml
version: '3.8'

services:
  orchestrator:
    build:
      target: development
    volumes:
      - ./services/orchestrator:/app
    command: npm run dev

  worker:
    build:
      target: development
    volumes:
      - ./services/worker:/app
    command: npm run dev
```

### Scripts

```bash
# scripts/start-local.sh
#!/bin/bash
docker-compose up -d
echo "Services starting..."
echo "Orchestrator: http://localhost:3000"
echo "Redis: localhost:6379"

# scripts/stop-local.sh
#!/bin/bash
docker-compose down

# scripts/logs.sh
#!/bin/bash
docker-compose logs -f ${1:-}

# scripts/reset.sh
#!/bin/bash
docker-compose down -v
docker-compose up -d
```

### Environment File

`.env.example`:
```env
# Redis
REDIS_URL=redis://localhost:6379

# Orchestrator
ORCHESTRATOR_PORT=3000
LOG_LEVEL=info

# Worker
WORKER_CONCURRENCY=2

# Authentication
API_KEY=your-api-key-here

# GitHub (for integrations)
GITHUB_TOKEN=your-github-token
```

## Acceptance Criteria

- [ ] `docker-compose up` starts all services
- [ ] Services can communicate
- [ ] Hot reload works in development
- [ ] Volume mounts for source code
- [ ] Health checks pass
- [ ] Clean shutdown with `docker-compose down`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
