Generated by /plan — do not hand-edit.

# Stack: 1202 — Optional llm-gateway (Bifrost) scaffolding

## Language / runtime

- TypeScript (ESM), Node >= 22
- Package: `packages/generacy` (`@generacy-ai/generacy`, main CLI)

## Frameworks & libraries (all pre-existing; no new dependencies)

- Commander — CLI flag wiring (`--llm-gateway` / `--no-llm-gateway` boolean pair on `launch` and `deploy`)
- `yaml` (`stringify`) — compose / cluster.yaml emission
- Zod — `ClusterYamlSchema` extension (`llmGateway: z.boolean().default(false)`) in `cli/commands/cluster/context.ts`
- `node:crypto` `randomBytes` — cluster-local token generation (`sk-bf-` + 48 hex)
- Vitest — unit + snapshot (golden-file) tests

## External runtime component

- Bifrost `maximhq/bifrost:v2.0.0` (Apache-2.0), pinned — config schema moves between majors;
  all P2-dogfood findings were verified against v2.0.0. Anthropic ingress at `/anthropic`
  (port 8080, cluster network only, no host port). Compose stanza is a port of
  tetrad-development#109 with recorded deltas (network name, token via `.env` interpolation).

## Integration points

- Emits canonical env vars consumed elsewhere in epic #1197: `GENERACY_LLM_GATEWAY_URL`
  (`src/config/loader.ts` warning path), `GENERACY_LLM_GATEWAY_TOKEN`
  (`src/cli/commands/doctor/checks/llm-gateway.ts`).
- Provider API keys come from operator-owned `.generacy/.env.local` (create-if-absent scaffold);
  no credentials-subsystem involvement (MVP).

## Notable technical decisions

- Toggle persisted in `.generacy/cluster.yaml` (`llmGateway: true`, key omitted when false);
  precedence: explicit flag > `GENERACY_LLM_GATEWAY_ENABLED=true` > persisted value > off.
- Token generate-once in `.env`, preserved verbatim on every re-scaffold (enabled or disabled);
  never a literal in config files — referenced as `env.GENERACY_LLM_GATEWAY_TOKEN`.
- Disabled path byte-identical to current scaffolder output (golden test); disabled re-scaffold
  of a previously-enabled cluster deletes nothing.
- Changeset: `@generacy-ai/generacy` minor.
