# Implementation Plan: Bootstrap CLAUDE.md, .speckit templates, .mcp.json

**Feature**: Set up foundational development infrastructure for AI-assisted development
**Branch**: `001-bootstrap-claude-md-speckit`
**Status**: Complete

## Summary

This feature bootstraps the generacy repository with the necessary configuration files and templates for AI-assisted spec-driven development. It establishes the project documentation (CLAUDE.md), specification templates (.specify/templates/), and MCP server configuration (.mcp.json).

## Technical Context

| Aspect | Details |
|--------|---------|
| Language | Markdown, JSON |
| Framework | Claude Code Plugin System |
| Dependencies | Context7 MCP, Playwright MCP |
| Target | Repository root configuration |

## Project Structure

```text
/workspaces/generacy/
├── CLAUDE.md                    # Project overview and development guidelines
├── .mcp.json                    # MCP server configuration
├── .gitignore                   # Git ignore rules
├── README.md                    # Project README
├── .claude/                     # Claude Code configuration
│   └── autodev.json             # Autodev configuration (optional)
├── .specify/
│   ├── templates/               # Speckit templates
│   │   ├── spec-template.md     # Feature specification template
│   │   ├── plan-template.md     # Implementation plan template
│   │   ├── tasks-template.md    # Task list template
│   │   ├── checklist-template.md# Quality checklist template
│   │   └── agent-file-template.md # Agent file generation template
│   └── memory/                  # Optional: constitution.md for governance
└── specs/                       # Feature specifications
    └── ###-feature-name/
        ├── spec.md
        ├── plan.md
        ├── tasks.md
        └── ...
```

## Files to Create/Modify

### 1. CLAUDE.md (Create)

Root-level development guidelines file containing:

| Section | Content |
|---------|---------|
| Project Overview | Generacy as orchestration layer connecting Agency and Humancy |
| Active Technologies | Markdown, JSON, MCP Protocol |
| Project Structure | Monorepo with orchestrator, workers, plugins |
| Commands | Build/test commands for future components |
| Code Style | Guidelines for TypeScript (future), Markdown |
| Manual Additions | Section for repo-specific guidance |

Template reference: `/workspaces/claude-plugins/CLAUDE.md`

### 2. .specify/templates/ (Create Directory + Files)

Copy templates from `/workspaces/claude-plugins/.specify/templates/`:

| File | Purpose |
|------|---------|
| `spec-template.md` | Feature specification structure |
| `plan-template.md` | Implementation plan structure |
| `tasks-template.md` | Task list format with phases |
| `checklist-template.md` | Quality checklist format |
| `agent-file-template.md` | CLAUDE.md generation template |

### 3. .mcp.json (Create)

MCP server configuration at repository root:

| Server | Purpose | Command |
|--------|---------|---------|
| context7 | Documentation and code context lookup | `npx -y @upstash/context7-mcp@latest` |
| playwright | Browser automation for testing | `npx -y @playwright/mcp@latest --headless` |

Template reference: `/workspaces/agency/.mcp.json`

## Implementation Approach

1. **CLAUDE.md Creation**: Create a comprehensive CLAUDE.md tailored for the generacy monorepo structure, documenting the project as an orchestration layer for AI-assisted development

2. **Template Setup**: Create `.specify/templates/` directory and copy all 5 template files from claude-plugins repository

3. **MCP Configuration**: Create `.mcp.json` with Context7 and Playwright servers configured identically to the agency repo

## Verification Steps

1. Verify CLAUDE.md exists and contains all required sections
2. Verify `.specify/templates/` contains all 5 template files
3. Verify `.mcp.json` is valid JSON with both MCP servers configured
4. Test `/speckit:specify` command creates features correctly

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| Copy templates directly | Ensures consistency with claude-plugins tooling |
| Use Context7 + Playwright | Standard MCP servers for documentation and testing |
| Match agency .mcp.json format | Proven configuration format |
| Include Manual Additions section | Allows future customization |

---

*Generated by speckit*
