# Feature Specification: Plugin: @generacy-ai/generacy-plugin-jira

**Branch**: `015-plugin-generacy-ai-generacy` | **Date**: 2026-02-06 | **Status**: Draft

## Summary

## Summary

Implement the Atlassian Jira integration plugin for Generacy.

## Parent Epic

#11 - Generacy Official Plugins

## Dependencies

- #2 - Generacy Core Package

## Features

### Issue Operations

```typescript
interface JiraPlugin {
  // Issue CRUD
  createIssue(params: CreateJiraIssueParams): Promise<JiraIssue>;
  getIssue(key: string): Promise<JiraIssue>;
  updateIssue(key: string, params: UpdateJiraIssueParams): Promise<JiraIssue>;
  transitionIssue(key: string, transitionId: string): Promise<void>;
  
  // Search
  searchIssues(jql: string): Promise<JiraIssue[]>;
  
  // Custom fields
  getCustomFields(): Promise<CustomField[]>;
  setCustomField(key: string, fieldId: string, value: unknown): Promise<void>;
  
  // Comments
  addComment(key: string, body: string): Promise<Comment>;
  
  // Sprints
  getActiveSprint(boardId: string): Promise<Sprint>;
  addToSprint(issueKey: string, sprintId: number): Promise<void>;
  
  // Webhooks
  handleWebhook(event: JiraWebhookEvent): Promise<void>;
}
```

### Jira-Specific Features

- JQL query support
- Workflow transitions
- Epic/Story/Task hierarchy
- Sprint management
- Custom field mapping
- Atlassian Document Format (ADF) for comments

### Configuration

```typescript
interface JiraConfig {
  host: string;                    // e.g., company.atlassian.net
  email: string;
  apiToken: string;
  projectKey: string;
  issueTypeMapping?: {
    feature: string;               // Issue type for features
    bug: string;                   // Issue type for bugs
    task: string;                  // Issue type for tasks
  };
  workflowMapping?: {
    todo: string;                  // Status ID
    inProgress: string;
    done: string;
  };
}
```

### Webhook Events

- `jira:issue_created`
- `jira:issue_updated`
- `jira:issue_assigned`
- `comment_created`

## Acceptance Criteria

- [ ] CRUD operations work
- [ ] JQL search works
- [ ] Workflow transitions work
- [ ] Custom fields supported
- [ ] Webhook events handled

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
