# Implementation Plan: Remove Role Selection from Bootstrap Wizard

**Feature**: Remove the "Role Selection" step (step 3) from the bootstrap wizard and all supporting cluster-side code
**Branch**: `582-problem-bootstrap-wizard-s`
**Status**: Complete

## Summary

Delete all cluster-level role configuration code from the `control-plane` package. Roles are workspace-level (`.agency/roles/`), not cluster-level. The wizard's "Role Selection" step, its `set-default-role` lifecycle action, the `default-role-writer` service, and the `/roles/:id` control-plane routes are all dead code that should be removed. This is a pure deletion — no new code is written.

## Technical Context

**Language/Version**: TypeScript (ESM), Node >=20
**Primary Dependencies**: `node:http`, `zod`, `yaml`
**Storage**: N/A (removing file-writing code)
**Testing**: Vitest (existing control-plane test suite)
**Target Platform**: Linux (in-cluster service)
**Project Type**: Monorepo package (`packages/control-plane`)
**Constraints**: Must not break existing tests or other lifecycle actions

## Constitution Check

No constitution file found at `.specify/memory/constitution.md`. Skipping gate.

## Project Structure

### Documentation (this feature)

```text
specs/582-problem-bootstrap-wizard-s/
├── spec.md              # Feature specification
├── plan.md              # This file
├── research.md          # Deletion map and rationale
├── data-model.md        # Schema changes
├── quickstart.md        # Verification guide
├── contracts/           # N/A (removing, not adding)
└── tasks.md             # Generated by /speckit:tasks
```

### Source Code (deletions)

```text
packages/control-plane/
├── src/
│   ├── routes/
│   │   └── roles.ts              # DELETE ENTIRE FILE
│   ├── services/
│   │   └── default-role-writer.ts # DELETE ENTIRE FILE
│   ├── router.ts                  # Remove roles import + 2 route entries
│   ├── routes/lifecycle.ts        # Remove set-default-role branch + imports
│   ├── schemas.ts                 # Remove set-default-role enum + body schema
│   └── index.ts                   # Remove re-exports
├── __tests__/
│   ├── routes/
│   │   ├── roles.test.ts          # DELETE ENTIRE FILE
│   │   └── lifecycle.test.ts      # Remove set-default-role test cases + mock
│   ├── services/
│   │   └── default-role-writer.test.ts  # DELETE ENTIRE FILE
│   ├── router.test.ts             # Remove role routing tests
│   └── integration/
│       └── all-routes.test.ts     # Remove role endpoint tests
```

## Implementation Phases

### Phase 1: Delete source files (2 files)
1. Delete `src/routes/roles.ts`
2. Delete `src/services/default-role-writer.ts`

### Phase 2: Update source files (4 files)
3. `src/schemas.ts` — Remove `'set-default-role'` from `LifecycleActionSchema`, delete `SetDefaultRoleBodySchema` and its type export
4. `src/router.ts` — Remove `handleGetRole`/`handlePutRole` import and the two `/roles/:id` route entries
5. `src/routes/lifecycle.ts` — Remove `SetDefaultRoleBodySchema` from import, delete `setDefaultRole` import, delete the `set-default-role` handler block
6. `src/index.ts` — Remove `SetDefaultRoleBodySchema` and `SetDefaultRoleBody` from re-exports

### Phase 3: Delete test files (2 files)
7. Delete `__tests__/routes/roles.test.ts`
8. Delete `__tests__/services/default-role-writer.test.ts`

### Phase 4: Update test files (2 files)
9. `__tests__/routes/lifecycle.test.ts` — Remove `setDefaultRole` mock and `set-default-role` test cases
10. `__tests__/router.test.ts` — Remove role routing test cases
11. `__tests__/integration/all-routes.test.ts` — Remove role endpoint test cases

### Phase 5: Verify
12. Run `tsc --noEmit` in control-plane package
13. Run test suite
14. Grep for zero remaining references (SC-001)

## Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| External consumer of `/roles/:id` | Low | Medium | Grep confirms no imports outside control-plane |
| Breaking lifecycle.ts remaining actions | Low | High | Careful surgical removal of only the set-default-role branch |
| Missing a reference | Low | Low | SC-001 grep verification catches stragglers |

## Complexity Tracking

No constitution violations — this is a pure deletion with no new abstractions.
