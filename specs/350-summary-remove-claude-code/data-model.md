# Data Model: Remove Claude Marketplace Plugin Install

**Feature**: 350-summary-remove-claude-code | **Date**: 2026-03-07

## Core Entities

### BuildConfig (existing — no changes)

```typescript
interface BuildConfig {
  workspaceRoot: string;  // Root of the pnpm workspace
  agencyDir: string;      // Path to agency repo (may not exist)
  // ... other fields unchanged
}
```

### Resolution Result (new internal concept)

```typescript
// Not a formal interface — just the return from resolveSpeckitCommandsDir()
type CommandsDir = string | null;
// When non-null: absolute path to directory containing .md command files
// When null: package not found in any tier
```

## File System Layout

### Input: Package Commands Directory

```text
@generacy-ai/agency-plugin-spec-kit/
└── commands/
    ├── specify.md
    ├── clarify.md
    ├── plan.md
    ├── tasks.md
    ├── implement.md
    ├── checklist.md
    ├── analyze.md
    ├── constitution.md
    └── taskstoissues.md
```

### Output: User Commands Directory

```text
~/.claude/
├── commands/          # Speckit command files copied here
│   ├── specify.md
│   ├── clarify.md
│   └── ...
└── (other claude config)

~/.claude.json         # MCP server config (unchanged)
```

## Resolution Priority

| Priority | Path | When Used |
|----------|------|-----------|
| 1 | `{workspaceRoot}/node_modules/@generacy-ai/agency-plugin-spec-kit/commands/` | tetrad-development workspace |
| 2 | `{agencyDir}/node_modules/@generacy-ai/agency-plugin-spec-kit/commands/` | agency repo workspace link |
| 3 | `{npm root -g}/@generacy-ai/agency-plugin-spec-kit/commands/` | External containers, global install |

## Validation Rules

- Source directory must exist and contain at least one `.md` file
- Target directory (`~/.claude/commands/`) is created if missing (`mkdirSync recursive`)
- Files are overwritten on copy (no merge logic needed)
- Error message if no resolution tier succeeds

---

*Generated by speckit plan — 2026-03-07*
