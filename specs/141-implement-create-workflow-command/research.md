# Research: Implement 'Create Workflow' Command

**Feature**: `141-implement-create-workflow-command`
**Date**: 2026-01-23

## Key Finding

**The implementation already exists.** During codebase exploration, I discovered that `commands/workflow.ts` contains a fully functional `createWorkflow()` function that satisfies all requirements.

## Existing Implementation Analysis

### `createWorkflow()` in `commands/workflow.ts:17-101`

The function implements:

1. **Workspace check** (lines 25-30)
   ```typescript
   const workspaceFolder = vscode.workspace.workspaceFolders?.[0];
   if (!workspaceFolder) {
     throw new GeneracyError(
       ErrorCode.DirectoryNotFound,
       'No workspace folder open. Please open a folder to create workflows.'
     );
   }
   ```

2. **Template selection** (lines 33-39)
   - Uses `showTemplateQuickPick()` from `templates.ts`
   - Displays all templates with icons and descriptions
   - Supports template preview (opens in side editor)

3. **Name input with validation** (lines 42-52)
   - Uses `validateWorkflowName()` helper
   - Enforces: starts with letter, alphanumeric + hyphens/underscores, max 64 chars

4. **Directory auto-creation** (lines 59-64)
   - Creates `.generacy/workflows/` if missing
   - Silently handles "directory exists" case

5. **Duplicate check** (lines 67-78)
   - Throws `GeneracyError` if file already exists

6. **File creation** (lines 80-92)
   - Loads template via `TemplateManager.getTemplate()`
   - Customizes with workflow name via `customizeTemplate()`
   - Writes UTF-8 encoded content

7. **Editor integration** (lines 97-100)
   - Opens created file in editor
   - Shows success message

## Template System

### `TemplateManager` in `templates.ts`

- **Initialization**: Called in `extension.ts:54-56`
- **Templates stored**: `resources/templates/*.yaml`
- **Available templates**:
  - `basic` - Single phase workflow
  - `multi-phase` - Setup/build/deploy phases
  - `with-triggers` - Webhook and schedule triggers

### `showTemplateQuickPick()` in `templates.ts:224-293`

- Custom quickpick with preview support
- Shows template content in side panel on hover
- Returns selected `TemplateMetadata` or undefined on cancel

## Why Handler Wasn't Connected

The `handleCreateWorkflow()` function in `extension.ts` was likely a placeholder during initial development. The actual implementation was built in `commands/workflow.ts` following the same pattern as other CRUD operations (rename, delete, duplicate).

## Decision

**Do not rewrite.** Simply wire up the existing implementation by:
1. Importing `createWorkflow` from `./commands/workflow`
2. Calling it from `handleCreateWorkflow()`

This approach:
- Reuses tested code
- Maintains consistency with other workflow CRUD commands
- Minimizes change footprint
- Follows existing error handling patterns

---

*Generated by speckit*
