# Feature Specification: Extend ProcessFactory with optional uid/gid

**Branch**: `426-goal-phase-6-spawn` | **Date**: 2026-04-12 | **Status**: Draft

## Summary

Phase 6 of the [spawn refactor](https://github.com/generacy-ai/tetrad-development/blob/develop/docs/spawn-refactor-plan.md#phase-6--extend-processfactory-with-uidgid-no-callers-use-it-yet). Additive extension to `ProcessFactory.spawn` options — adds optional `uid` / `gid` pass-through so the primitive is stable before the follow-on credentials architecture work wires them up.

## Scope

- Add optional `uid?: number` and `gid?: number` to the `options` parameter of `ProcessFactory.spawn` at `packages/orchestrator/src/worker/types.ts:269`.
- Update both implementations to pass them through to `child_process.spawn`:
  - `defaultProcessFactory` at `packages/orchestrator/src/worker/claude-cli-worker.ts:25-54`
  - `conversationProcessFactory` at `packages/orchestrator/src/conversation/process-factory.ts:10-40`
- No callers pass uid/gid yet; defaults remain `undefined`, meaning behavior is byte-identical to today.

## User Stories

### US1: Infrastructure readiness for process identity control

**As a** platform engineer working on the credentials architecture,
**I want** `ProcessFactory.spawn` to accept optional `uid` and `gid` parameters,
**So that** future work can spawn child processes under specific Unix user/group identities without modifying the factory interface.

**Acceptance Criteria**:
- [x] `ProcessFactory` interface accepts optional `uid` and `gid` in its options
- [ ] Both implementations (`defaultProcessFactory`, `conversationProcessFactory`) forward `uid`/`gid` to `child_process.spawn`
- [ ] Omitting `uid`/`gid` produces identical behavior to the current code
- [ ] No existing callers are modified

### US2: Testable uid/gid pass-through

**As a** developer writing the credentials integration,
**I want** unit tests proving `uid`/`gid` are correctly forwarded,
**So that** I can confidently wire up callers knowing the primitive works.

**Acceptance Criteria**:
- [ ] Unit tests verify `uid`/`gid` are included in `child_process.spawn` options when provided
- [ ] Unit tests verify `uid`/`gid` are absent from `child_process.spawn` options when omitted

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | Add `uid?: number` to `ProcessFactory.spawn` options type | P1 | At `types.ts:273` |
| FR-002 | Add `gid?: number` to `ProcessFactory.spawn` options type | P1 | At `types.ts:273` |
| FR-003 | `defaultProcessFactory.spawn` forwards `uid`/`gid` to `child_process.spawn` | P1 | In `claude-cli-worker.ts` |
| FR-004 | `conversationProcessFactory.spawn` forwards `uid`/`gid` to `child_process.spawn` | P1 | In `process-factory.ts` |
| FR-005 | When `uid`/`gid` are `undefined`, they must not appear in the spawn options object | P1 | Preserves current behavior exactly |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | All existing tests pass | 100% | `pnpm test` in orchestrator package |
| SC-002 | New uid/gid forwarding tests pass | 100% | Unit tests for both factory implementations |
| SC-003 | No callers modified | 0 call sites changed | `git diff` shows no caller changes |

## Assumptions

- `child_process.spawn` accepts `uid` and `gid` in its options (Node.js built-in support).
- Wave 0 refactoring is complete and the `ProcessFactory` interface is stable.
- Both factory implementations follow the same pattern and can be updated identically.

## Out of Scope

- Wiring `uid`/`gid` from `AgentLauncher` or any caller (belongs to the credentials architecture follow-on).
- Any other `ProcessFactory` option additions.
- Permission/capability checks for running as different uid/gid.

## Dependencies

- Depends on Wave 0.
- Parallel-safe with the other Wave 1 issues (no file overlap).

## References

- Parent tracking: #423

---

*Generated by speckit*
