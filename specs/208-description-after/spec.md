# Feature Specification: ## Description

After the orchestrator's validate phase completes successfully (all phases done), the draft PR should be automatically marked as ready for review

**Branch**: `208-description-after` | **Date**: 2026-02-21 | **Status**: Draft

## Summary

## Description

After the orchestrator's validate phase completes successfully (all phases done), the draft PR should be automatically marked as ready for review. Currently the PR remains in draft state indefinitely, requiring manual intervention.

## Current Behavior

1. Orchestrator runs phases: specify → clarify → plan → tasks → implement → validate
2. After each phase, `PrManager.commitPushAndEnsurePr()` commits, pushes, and ensures a **draft** PR exists
3. When all phases complete, `onWorkflowComplete()` only removes the `agent:in-progress` label
4. The PR stays as a draft — no one is notified it's ready

## Expected Behavior

After the validate phase passes (or when `PhaseLoop` returns `completed: true`), the orchestrator should:

1. Mark the PR as ready for review (remove draft status)
2. Optionally request reviewers (if configured)

## Suggested Implementation

Add a `markReadyForReview()` method to `PrManager` that calls the GitHub API to convert the draft PR to ready. Invoke it from `claude-cli-worker.ts` in the `loopResult.completed` branch (line 227), after `onWorkflowComplete()`.

```typescript
// claude-cli-worker.ts, after line 228
if (loopResult.completed) {
  await labelManager.onWorkflowComplete();
  await prManager.markReadyForReview();
  // ...
}
```

## Files

- `packages/orchestrator/src/worker/pr-manager.ts` — add `markReadyForReview()` method
- `packages/orchestrator/src/worker/claude-cli-worker.ts` — call it on completion (line 227)

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Assumptions

- [Assumption 1]

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
