# Feature Specification: G1: Design tasks-to-issues workflow YAML

**Branch**: `162-g1-design-tasks-issues` | **Date**: 2026-02-01 | **Status**: Draft

## Summary

## Related Epic
Part of generacy-ai/agency#139

## Description
Design the Generacy workflow YAML for converting tasks to backlog issues with human review checkpoints.

## Acceptance Criteria
- [ ] Design workflow YAML structure
- [ ] Define inputs (tasks.md path, grouping strategy, provider)
- [ ] Define Humancy checkpoint for review before creation
- [ ] Support dry-run preview
- [ ] Define outputs (created issue IDs and URLs)
- [ ] Document workflow triggers

## Workflow Design

```yaml
# workflows/tasks-to-issues.yaml
name: Tasks to Issues
description: Convert tasks.md into backlog tickets with human review

inputs:
  feature_dir:
    description: Feature directory containing tasks.md
    type: string
    required: true
  grouping:
    description: Grouping strategy
    type: string
    enum: [per-task, per-story, per-phase]
    default: per-task
  provider:
    description: Backlog provider override (auto-detected from repo config if not specified)
    type: string
    enum: [github, jira, shortcut, local]
    # Auto-detection: .github presence implies GitHub (Q3)
  dry_run:
    description: Preview only
    type: boolean
    default: false

steps:
  - id: parse_tasks
    name: Parse tasks.md
    action: spec_kit.get_paths
    with:
      feature_dir: ${{ inputs.feature_dir }}

  - id: validate_tasks
    name: Validate task dependencies
    action: agent.invoke
    with:
      prompt: |
        Read and validate tasks.md at ${{ steps.parse_tasks.outputs.tasksFile }}.
        Check for circular dependencies and missing references.

  - id: preview
    name: Generate issue preview
    action: spec_kit.tasks_to_issues
    with:
      feature_dir: ${{ inputs.feature_dir }}
      grouping: ${{ inputs.grouping }}
      dry_run: true

  - id: review
    name: Human review checkpoint
    action: humancy.request_review
    with:
      artifact: |
        ## Issues to Create
        ${{ steps.preview.outputs.summary }}
      context: Review the planned issues before creation
      # No timeout - workflow waits indefinitely for human approval (Q1)

  - id: create
    name: Create issues
    if: ${{ steps.review.approved && !inputs.dry_run }}
    action: spec_kit.tasks_to_issues
    with:
      feature_dir: ${{ inputs.feature_dir }}
      grouping: ${{ inputs.grouping }}
      dry_run: false
      include_feature_path: true  # Link back to source spec (Q4)

outputs:
  issues:
    description: Created issue details (ID, URL, title, provider type)
    value: ${{ steps.create.outputs.issues }}
    # Standard output format: ID, URL, title, and provider type (Q5)
```

## Triggers

The workflow can be triggered by:
1. Manual invocation from Generacy UI
2. Workflow step in larger SDLC workflow
3. Event trigger (e.g., tasks.md file committed)

## Dependencies
- Agency spec-kit plugin (D5 - tasks_to_issues tool)
- Humancy plugin integration

## Files to Create
- `workflows/tasks-to-issues.yaml`
- `docs/workflows/tasks-to-issues.md`

## User Stories

### US1: [Primary User Story]

**As a** [user type],
**I want** [capability],
**So that** [benefit].

**Acceptance Criteria**:
- [ ] [Criterion 1]
- [ ] [Criterion 2]

## Functional Requirements

| ID | Requirement | Priority | Notes |
|----|-------------|----------|-------|
| FR-001 | [Description] | P1 | |

## Success Criteria

| ID | Metric | Target | Measurement |
|----|--------|--------|-------------|
| SC-001 | [Metric] | [Target] | [How to measure] |

## Design Decisions (from Clarifications)

1. **Humancy Review Timeout**: No timeout - workflow waits indefinitely for human approval
2. **Error Handling**: Fail fast - any step failure stops the entire workflow
3. **Provider Auto-Detection**: Detect from repository configuration (e.g., .github presence implies GitHub)
4. **Issue Linking**: Add feature directory path in issue body to link back to source spec
5. **Output Format**: Standard fields - ID, URL, title, and provider type

## Assumptions

- Repository configuration can be reliably detected for provider auto-detection
- Human reviewers will respond to review requests (no timeout needed)
- Downstream consumers can work with standard output format

## Out of Scope

- [Exclusion 1]

---

*Generated by speckit*
