# Quickstart: Update Getting-Started Docs for Cluster Base Repo Approach

## Prerequisites

- Access to the `generacy` repository on branch `355-summary-update-external-facing`
- Familiarity with Docusaurus markdown conventions
- Access to the [migration plan](https://github.com/generacy-ai/tetrad-development/blob/develop/docs/cluster-base-migration-plan.md) for technical details

## Files to Create

| File | Action | Description |
|------|--------|-------------|
| `docs/docs/getting-started/cluster-setup.md` | Create | New page documenting cluster base repo setup |

## Files to Modify

| File | Action | Description |
|------|--------|-------------|
| `docs/docs/getting-started/dev-environment.md` | Modify | Add cross-reference to cluster-setup page |
| `docs/docs/getting-started/project-setup.md` | Modify | Add onboarding PR section, mention cluster-base.json |

## Files Unchanged

| File | Reason |
|------|--------|
| `docs/docs/getting-started/multi-repo.md` | No `cluster-templates` references found; no changes needed |

## Implementation Order

1. **Read migration plan** — Get authoritative details for git commands and `cluster-base.json` schema
2. **Create `cluster-setup.md`** — Main deliverable with fork chain docs, setup workflow, update workflow
3. **Update `project-setup.md`** — Add onboarding PR and `cluster-base.json` content
4. **Update `dev-environment.md`** — Add cross-reference to cluster-setup
5. **Verify** — Run acceptance criteria checks

## Verification Commands

```bash
# SC-001: No cluster-templates references
grep -r "cluster-templates" docs/docs/getting-started/
# Expected: no output

# Check new page exists
ls docs/docs/getting-started/cluster-setup.md

# Check for broken links (if Docusaurus build is available)
cd docs && npm run build
```

## Acceptance Checklist

- [ ] No references to `cluster-templates` in getting-started docs
- [ ] Fork chain documented with both variants (`cluster-base`, `cluster-microservices`)
- [ ] Update workflow (`git fetch` + `git merge`) documented step-by-step
- [ ] Manual setup (`git remote add` + `--allow-unrelated-histories`) documented
- [ ] Links point to new base repos
- [ ] `cluster-base.json` explained
- [ ] Onboarding PR process described in project-setup.md

---

*Generated by speckit*
