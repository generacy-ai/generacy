services:
  orchestrator:
    build:
      target: development
    volumes:
      - ./services/orchestrator:/app
    command: npm run dev

  worker:
    build:
      target: development
    volumes:
      - ./services/worker:/app
    command: npm run dev
